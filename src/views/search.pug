extends base
include mixins/video

block content
  form(method='GET')
    input(placeholder='Search by title', name='keyword', type='title')
    input(type='submit', value='Search now')

  div
    each video in videos
      +video(video)
