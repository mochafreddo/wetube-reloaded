extends ../base
include ../mixins/video

block content
  .profile__data
    if loggedInUser.avatarUrl === ""
      span 😄
    else
      img.avatar(src=user.avatarUrl, crossorigin)
    h3= user.name
  .video-grid
  each video in user.videos 
    +video(video)
  else
    span.empth__message No videos found
